# Copy to .env for docker compose (do not commit .env with real secrets)
POSTGRES_USER=nexuscore
POSTGRES_PASSWORD=change_me
POSTGRES_DB=nexuscore
DOCKER_FRONTEND_PORT=8081

# n8n (optional — used by backend-service in docker-compose)
# N8N_WEBHOOK_URL=http://n8n:5678/webhook/hr-events
